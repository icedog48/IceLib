                    @{ var hasSchema = Model.Schema.TypeAsInt != 0; }
                    "@Model.ResponseCodeAsInt" : {
                        "description" : "@Model.Description" @( hasSchema ? ", " : string.Empty)
                        @if (hasSchema)
                        {
                        @:"schema" : { 
                        @Include("_Schema", Model.Schema)
                        @:}
                        } 
                    }
