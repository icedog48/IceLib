{
    "swagger": "2.0",

    "info": {
        @Include("_Info", Model.Info)
    },
    
    "paths": {
        @{ var counter = 0; }
        @foreach (var path in Model.Paths)
        {
            @Include("_Path", path) @(++counter == Model.Paths.Count ? string.Empty : ", ")
        }
    }
}
